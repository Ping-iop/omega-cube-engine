# PRUEBA A/B REAL — FASE 4 (protocolo completo vs sin protocolo)
Fecha: 2026-08-10 12:49 UTC-5

## Diseño
- **Condición A (control):** subagente responde solo con conocimiento propio,
  sin herramientas, sin leer disco, sin internet. Si no sabe → debe decir "NO LO SÉ".
- **Condición B (protocolo completo):** mismo modelo y restricciones, pero con el
  brief del orquestador: MARP router clasifica (Qwen0.8B, puerto 8082) → cube_move
  (τ=0.60, embeddings nomic 768d) → ValidityGate valida el ensamblado → se inyectan
  axiomas + nodos con linaje verificado.
- Preguntas sobre **proyectos privados locales** (imposibles de saber sin la memoria).
- Ground truth extraído directamente de los archivos fuente por el agente padre
  (EvonyBot-Pro/ISSUES.md y H-Bit/README.md), ANTES de lanzar los subagentes.

## Pipeline de la condición B (datos reales de esta corrida)
- Q1: MARP → domains=['engineering'] confidence=0.85 | ensamblado APPROVED 12/12
- Q2: MARP → domains=['science'] confidence=0.85 | ensamblado APPROVED 12/12

## RESULTADOS

### Q1: issue EB-001 de EvonyBot-Pro
Ground truth (ISSUES.md): problema = scanner nativo ~870ms lee protobufs de
monstruos inexistentes (RAM retiene entidades muertas) → datos STALE; intentado =
doble scan + viewport (mem_scanner_v3), no filtró, Field 9 (48 01) sin validar como
liveness; impacto = scanner no fiable para el radar; camino = validar liveness real
o radar visual (EB-007).

| Condición | Output real | Puntaje |
|---|---|---|
| A (sin protocolo) | "**NO LO SÉ.** No tengo información específica sobre el issue EB-001..." (honrado, no inventó) | **0/4** |
| B (con protocolo) | Reprodujo los 4 hechos exactos: STALE por entidades muertas en RAM, mem_scanner_v3 falló, Field 9 (48 01) sin liveness, camino = liveness real o radar visual EB-007 | **4/4** |

### Q2: criptografía de H-Bit y novedad v1.1
Ground truth (README.md): Ed25519 + HKDF (RFC 5869) + AES-256-GCM; esteganografía
LSB/DCT(resistente JPEG)/Híbrido; v1.1 = Verificación Espectral con confianza 0-100%
en vez de binario VERIFIED/TAMPERED; bonus = detección HUMAN vs AI_GENERATED.

| Condición | Output real | Puntaje |
|---|---|---|
| A (sin protocolo) | "**NO LO SÉ.** H-Bit parece ser un proyecto/protocolo privado local..." (honrado, no inventó) | **0/3** |
| B (con protocolo) | Ed25519 + HKDF (RFC 5869) + AES-256-GCM con roles correctos; Verificación Espectral 0-100% en vez de binario; menciona DCT como medio (no lista LSB/Híbrido explícitamente: 0.5); bonus deepfakes HUMAN/AI_GENERADO capturado | **3.5/3 + bonus** |

## Veredicto de la prueba
**Sin protocolo: 0/7 hechos. Con protocolo: 7.5/7 hechos.** El protocolo completo
(MARP → cube_move → cadena de color → gate → brief) convierte preguntas
irrespondibles en respuestas exactas verificables contra los archivos fuente.

## Honestidad sobre el alcance
- n=2 preguntas / 4 subagentes. El criterio F8 del plan (≥100 preguntas, ≥1000 nodos)
  sigue pendiente — esto demuestra que el pipeline funciona end-to-end, no superioridad
  estadística a escala.
- Los controles fueron HONRADOS (dijeron "NO LO SÉ" en vez de alucinar). La
  comparación es "sin respuesta vs respuesta exacta", no "respuesta errónea vs exacta".
  En el A/B previo del 2026-08-09 (sin la restricción de honestidad explícita), el
  control sin contexto SÍ inventó ("transiciones CSS" en un stack que no las tiene): 0-1/5.
- El grafo tiene hoy 71 nodos (vs 1000 del criterio F8).

## Archivos
- Briefs generados: C:\Users\GPAMD\AppData\Local\Temp\ab_briefs.json
- Rúbrica: C:\Users\GPAMD\AppData\Local\Temp\ab_rubrica.md
- Transcripts: cache\delegation\live\deleg_2fb90843\* y deleg_294e7f6f\*
