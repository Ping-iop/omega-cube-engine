# PRUEBAS FASE 3 — Gate de Validez Integrado (Fórmula F6)
Fecha: 2026-08-09 23:40 UTC-5

## Implementación
- `omega_cube/validity_gate.py` — ValidityGate: APPROVED/VETOED/FLAGGED por
  linaje de color real (verify_lineage por nodo) + gray-scale del ensamblado.
- `omega_cube/cube_move.py` — CubeMover acepta color_chain opcional; con él,
  `_validate_assembly` usa ValidityGate (sin él, fallback jerárquico Fase 1).
- Gray-Scale Validation EXISTENTE intacta (extender, no reemplazar): grayscale.py
  sin modificar (md5 7f5feabdd8960a32ce5a316ef4adb946); el gate añade el eje de
  PROCEDENCIA y reporta ambos.

## PRUEBA 3.1 — respuesta con linaje completo → APPROVED
```
INPUT: cadena real SISTEMA completa: axioma OMEGA_CUBE → MARP → MARP pipeline
       → Diffusion Sampler (profundidades 0,1,2,3; saturaciones 1.0/0.7/0.49/0.343)
OUTPUT REAL:
GATE: APPROVED — linaje completo en 4/4 nodos
  [APPROVED] dc7150d72e6e9ca5 depth=0 sat=1.000: dc7150d72e6e
  [APPROVED] 411b5d4f5267c1bc depth=1 sat=0.700: dc7150d72e6e → 411b5d4f5267
  [APPROVED] d6183d28a1909662 depth=2 sat=0.490: dc7150d72e6e → 411b5d4f5267 → d6183d28a190
  [APPROVED] 713586c5c44b0098 depth=3 sat=0.343: dc7150d72e6e → 411b5d4f5267 → d6183d28a190 → 713586c5c44b
  gray-scale: min=40% avg=42%
ESPERADO: APPROVED + cadena de axiomas mostrada
VEREDICTO: PASS
```

## PRUEBA 3.2 — respuesta con linaje roto → FLAGGED + dónde se rompe
```
INPUT: 2 nodos sanos + nodo con lineage a "AXIOMA_FANTASMA_QUE_NO_EXISTE"
OUTPUT REAL:
GATE: FLAGGED — linaje parcial: 2/3 verificados; rotura en: ['TEST.LINAJE.ROTO']
  [APPROVED] dc7150d72e6e9ca5 depth=0 sat=1.000
  [APPROVED] 411b5d4f5267c1bc depth=1 sat=0.700
  [FLAGGED ] TEST.LINAJE.ROTO.FASE3 — linaje roto: origen AXIOMA_FANTASMA_
             NO está en el registro de axiomas
ESPERADO: FLAGGED + mostrar dónde se rompe
VEREDICTO: PASS
```

## PRUEBA 3.3 — Gray-Scale existente sigue funcionando
```
INPUT: mismos inputs al validador (mismo nodo, query, axioms), 2 corridas
OUTPUT REAL:
  corrida 1: composite=18.8825 dims={factuality:10.0, relevance:0.0,
             recency:25.5, coherence:50.0, provenance:40.0, specificity:26.7}
  corrida 2: composite=18.8825 dims= idénticos → determinismo EXACTO
  verify_against_axioms funciona; partial_evidence_score(H-Bit) funciona
  grayscale.py sin modificar (fuera de los archivos tocados hoy)
  El gate nuevo CONSUME gray-scale: min=52.8% avg=56.0% reportados
ESPERADO: mismos resultados que antes (sin regresión)
VEREDICTO: PASS
Nota: una medición previa comparó composite con/sin axioms en la evaluación
(inputs DISTINTOS → outputs distintos, que es el comportamiento correcto del
validador, no una regresión). La prueba correcta es misma-entrada/misma-salida.
```

## Verificación cruzada: cube_move con gate integrado
```
INPUT: query='como funciona el motor omega-cube y su pipeline MARP' k=10 τ=0.60
OUTPUT REAL: Veredicto cube_move: FLAGGED — linaje parcial 4/10 verificados
  [APPROVED] OMEGA_CUBE axioma, MARP, HERMES_AGENT, MARP pipeline
  [FLAGGED ] 6 nodos orfanos (dominios sin axioma: HERMES, DEV, PROYECTO, EVONY)
  Gate gray-scale: min=40% avg=45%
INTERPRETACIÓN: comportamiento correcto — el gate reporta exactamente qué nodos
  carecen de linaje. El grafo tiene 41 nodos orfanos porque solo 2 dominios
  tienen axiomas; poblar axiomas subirá la tasa de APPROVED (Fase 4).
```
